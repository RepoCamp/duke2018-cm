define service{
        use                     generic-service
        host_name               {{ [hostname, domain] | join('.') }}
        service_description     connection to emory shib
        check_command           check_nrpe!check_emory_shib
        }
