define service{
        use                     generic-service
        host_name               {{ [hostname, domain] | join('.') }}
        service_description     shibboleth_sp
        check_command           check_nrpe!check_shib_SP
        }
