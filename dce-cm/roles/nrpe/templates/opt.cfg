define service{
        use                     generic-service
        host_name               {{ [hostname, domain] | join('.') }}
        service_description     /opt disk space
        check_command           check_nrpe!check_opt
        }
