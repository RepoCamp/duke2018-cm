define service{
        use                     generic-service
        host_name               {{ [hostname, domain] | join('.') }}
        service_description     Solr
        check_command           check_nrpe!check_solr
        event_handler           nrpe_event_handler!restart_solr
        }
