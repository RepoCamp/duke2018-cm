define service{
        use                     generic-service
        host_name               {{ [hostname, domain] | join('.') }}
        service_description     search_results
        check_command           check_search_results
        }
